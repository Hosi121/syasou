{
  "name": "syasou",
  "version": "1.0.0",
  "description": "A quiet, tactile train-window workspace.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "node scripts/artifacts.mjs && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "moon": "node scripts/moon.mjs",
    "build:moonbit": "node scripts/build.mjs",
    "check:moonbit": "npm run moon -- check --deny-warn && npm run moon -- fmt --check",
    "check:generated": "node scripts/build.mjs --check",
    "test": "npm run test:moonbit && npm run test:wasm",
    "test:moonbit": "node scripts/moon.mjs test",
    "test:wasm": "node --test tests/wasm/*.test.mjs",
    "test:browser": "playwright test",
    "test:browser:mobile": "playwright test --config playwright.mobile.config.js",
    "assets:icons": "node scripts/assets/icons.mjs",
    "assets:fonts": "node scripts/assets/fonts.mjs",
    "assets:grain": "node scripts/assets/grain.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "imports": {
    "#syasou/browser-host": "./src/generated/moonbit/browser-host.js"
  },
  "dependencies": {
    "@fontsource/dm-mono": "^5.3.0",
    "@fontsource/shippori-mincho": "^5.3.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0",
    "@tailwindcss/vite": "^4.3.3",
    "tailwindcss": "^4.3.3",
    "vite": "^8.3.0",
    "parse5": "^8.0.0"
  },
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  }
}
